// Expo's AppEntry resolves the root component from this path, so the file has
// to stay here. The application itself lives under src/ with everything else;
// see src/app/app.tsx.
export { default } from "./src/app/app";
