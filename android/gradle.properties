# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# React Native native builds are memory-intensive; 4 GB prevents mid-compile OOM.
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Shrink the release build with R8: strip unused Kotlin/Java code and unused
# resources. The release buildType already wires these to proguard-rules.pro.
# Release-only, so debug builds and development are unaffected. If a release
# build ever misbehaves at runtime, add a keep-rule in app/proguard-rules.pro
# or set these back to false.
android.enableMinifyInReleaseBuilds=true
android.enableShrinkResourcesInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=arm64-v8a
#
# This is the DEVELOPMENT set, and it is the only control over which ABIs a
# build produces - the abiFilters in app/build.gradle merge rather than override,
# so a build type cannot narrow it. See the long note in the release buildType.
#
# arm64-v8a: modern Android devices (2016+).
# armeabi-v7a: Android Go and entry-level 32-bit handsets, still in use across
#   the markets this app targets and unable to install an arm64-only build.
# x86_64: Android emulators on x86_64 hosts. Kept here so `npm run android` on an
#   emulator needs no flag, and dropped from release builds by the override in
#   the release workflows.
# x86 is excluded: no shipping device uses it and no current emulator needs it.
reactNativeArchitectures=arm64-v8a,armeabi-v7a,x86_64

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Use this property to enable edge-to-edge display support.
# This allows your app to draw behind system bars for an immersive UI.
# Note: Only works with ReactActivity and should not be used with custom Activity.
edgeToEdgeEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# NODE_ENV is required by expo-constants during Gradle builds.
# Set to 'development' for debug builds so .env files are resolved correctly.
systemProp.NODE_ENV=development

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

expo.inlineModules.watchedDirectories=[]
android.minSdkVersion=26